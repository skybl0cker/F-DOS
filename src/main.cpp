// main.cpp
#include <iostream>
#include "shell.h" // Include shell.h to make the Shell class visible

int main() {
    // Create the Shell object and start the shell
    Shell shell;
    shell.start();
    return 0;
}
